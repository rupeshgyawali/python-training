# String methods: len(<str>), <str>.lower(), <str>.upper(), <str>.count(<str>), <str>.find(<str>), <str>.replace(<st>to_replace, <str>replaced_with)
# String formating: f'{x} {<str>.upper()}', '{} {}'.format(value1, value2)
# String indexes and slicing: [index], [start:stop], [start:stop:step], [-1], [:-1], [::-1]
# Palindrome String, Number
# List: len(), [index], [start:stop], [start:stop:step], [-1], [:-1], [::-1]
# List adding and removing values: <lst>.append(value), <lst>.insert(index, value), <lst>.extend(<lst>), <lst>.remove(value), <lst>.pop()
# List ordering: <lst>.reverse(), <lst>.sort(reverse=False), sorted(<lst>, reverse=False) --> new <lst>
# min(<lst>), max(<lst>), sum(<lst>), <lst>.index(value)
# value in <lst> --> bool, looping through list, enumerate(<lst>, start=0)
# <str>.join(<lst>), <str>.split(<str>)
# Average marks calculate
# Tuples: immutable, <lst> = <lst> is referenced, <tuple>=<tuple> is not referenced, similar to <lst> but  cannot change.
# Sets: unordered and no duplicates, <set>.intersection(<set>), <set>.difference(<set>), <set>.union(<set>)
# Empty list: list(), [] ; Empty tuple: tuple(), () ; Empty set: set()
# Dictionaries: key-value pairs, <dict>[key], <dict>.get(key, None)
# Adding and updating values: <dict>[key] = value, <dict>.update(<dict>)
# Deleting values: del <dict>[key], <dict>.pop(key)
# Keys, Values, Items: len(<dict>), <dict>.keys(), <dict>.values(), <dict>.items()
# Looping through dict: for key in <dict>, for key, value in <dict>.items()
# Count number of each vowels in a paragraph and print
# Loops: for, while, break, continue, nested, infinite
# Multiplication table, First 20 Prime numbers
# False values: False, None, [], (), {}, '', 0, 0.0
# is operator, id()
# Functions: pass, func_name, func_name(), docstrings, return and arguments, default argument
# Argument packing and unpacking: *args, **kwargs
# Importing modules: import sys, sys.path, import random, random.choice(<lst>), random.randint(a,b),
#         import math, math.radians(degree)/.degrees(radian), math.sin(rads)/cos/tan
#         import os, os.getcwd(), os.__file__,
# OOP: pass,

# Swap case,
